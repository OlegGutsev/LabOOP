#pragma once
#include "Harbor.h"
class Controller
{
public:
	float getAverageDisplacment(Harbor*);   //SailingShip
	float getAverageCapacity(Harbor*);     //SteamShip
	void getQualityCaptain(Harbor*, int); // Captain's age 
private:
};
